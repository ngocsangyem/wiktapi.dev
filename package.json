{
  "name": "@wordictapi/root",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "prepare": "lefthook install",
    "ready": "vite fmt && vite lint --type-aware && vite run test -r && vite run build -r",
    "dev:api": "vp run @wordictapi/api#dev",
    "dev:dashboard": "vp run @wordictapi/dashboard#dev",
    "dev:docs": "vp run @wordictapi/docs#dev",
    "build:api": "vp run @wordictapi/api#build",
    "build:dashboard": "vp run @wordictapi/dashboard#build",
    "build:docs": "vp run @wordictapi/docs#build"
  },
  "devDependencies": {
    "lefthook": "^2.1.1",
    "shadcn-vue": "catalog:",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@10.30.0",
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild",
      "lefthook"
    ]
  }
}
